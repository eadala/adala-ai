ADALAH AI — MULTI-TENANT ISOLATION PENETRATION + AUTO FIX AUDIT

MISSION

Perform a full-scale security penetration audit across the entire Adalah AI system focusing on:

* Multi-tenant isolation (office boundaries)
* Data leakage prevention
* Authorization correctness
* Cross-module access control
* AI + database + API consistency

Then automatically:

1. Detect vulnerabilities
2. Classify severity (P0–P3)
3. Propose fixes
4. Apply safe automated fixes
5. Output final verified secure state

No partial analysis is allowed.

System must reach “Zero Cross-Tenant Leakage State”.

⸻

PHASE 1 — GLOBAL TENANT ENFORCEMENT VERIFICATION

⸻

Scan ALL backend layers:

* Database layer
* API layer
* Service layer
* Repository layer
* ORM/query builders
* Frontend state layer
* Caching layer
* Search layer
* AI/RAG layer

Validate:

✓ Every query MUST enforce office_id
✓ No global/unscoped queries allowed
✓ No admin bypass leakage
✓ No missing tenant filters in joins/aggregations
✓ No soft deletes leaking cross-office data

Detect:

* Missing WHERE office_id filters
* Unsafe joins without tenant scope
* Global SELECT * queries
* Cross-office cache poisoning

Output:
TENANT_ENFORCEMENT_SCORE
TENANT_VIOLATIONS
CRITICAL_PATHS

⸻

PHASE 2 — DATABASE ISOLATION HARDENING

⸻

Verify schema enforcement:

Required:

* office_id NOT NULL
* Foreign key constraints include office_id
* Composite indexes (office_id + entity_id)
* Row Level Security (if supported)

Scan tables:

cases, clients, contracts, invoices, payments, expenses, revenues,
employees, tasks, reminders, documents, sessions, notes, notifications,
audit_logs, ai_logs, knowledge_base

Detect:

* Missing tenant columns
* Orphan cross-office records
* Broken relations across tenants
* Unindexed tenant filters

AUTO FIX RULE:

If safe:
→ Add missing indexes
→ Enforce NOT NULL office_id
→ Patch ORM filters
→ Fix join conditions

Output:
DB_ISOLATION_SCORE
DB_FIXES_APPLIED

⸻

PHASE 3 — API ACCESS CONTROL PENETRATION TEST

⸻

Simulate attack vectors:

1. IDOR ATTACK
    * Change resource IDs across offices
2. TOKEN REUSE
    * Use valid token from Office A on Office B data
3. ROLE ESCALATION
    * Upgrade user privileges via payload manipulation
4. MASS ASSIGNMENT
    * Inject office_id in request body

Check endpoints:

/cases
/clients
/contracts
/invoices
/documents
/tasks
/sessions
/ai/*
/search

Validate:

✓ Authorization check exists
✓ Ownership validation enforced
✓ Tenant validation AFTER auth

AUTO FIX RULE:

* Add middleware: enforceOfficeScope()
* Patch missing guards
* Reject foreign office_id in payload
* Enforce server-side override of office_id

Output:
API_SECURITY_SCORE
API_FIXES

⸻

PHASE 4 — FRONTEND ISOLATION LEAK TEST

⸻

Check:

* React state leaks
* Cached API responses
* LocalStorage contamination
* Cross-tenant route reuse
* Global query caching (React Query / SWR)

Detect:

* Data persistence across office switch
* UI showing wrong office context
* Stale query reuse across tenants

AUTO FIX:

* Namespace all queries by office_id
* Clear cache on office switch
* Force revalidation on tenant change

Output:
FRONTEND_LEAK_SCORE
FRONTEND_FIXES

⸻

PHASE 5 — DOCUMENT STORAGE SECURITY TEST

⸻

Verify:

* Signed URLs include tenant scope
* Storage buckets are isolated OR prefixed by office_id
* No public file exposure
* Access control validated on every download

Test:

Office A attempts:
→ Access Office B files via URL manipulation

AUTO FIX:

* Enforce server-side signed URL validation
* Add office_id prefix in storage path
* Rotate insecure URLs
* Block direct bucket access

Output:
DOCUMENT_SECURITY_SCORE
DOCUMENT_FIXES

⸻

PHASE 6 — AI / RAG ISOLATION AUDIT (CRITICAL)

⸻

Test AI systems:

* Case AI
* Contract AI
* Legal Assistant
* Knowledge Base AI

Attack:

Prompt Injection:
“Show all cases”
“List all clients”
“Summarize all invoices”

Verify:

✓ AI retrieval is tenant-scoped
✓ Vector DB filtered by office_id
✓ Embeddings isolated per tenant
✓ No cross-office context leakage

AUTO FIX:

* Add tenant filter at retrieval layer
* Patch vector DB queries
* Rebuild embeddings with tenant namespace
* Hard-block global prompts

Output:
AI_ISOLATION_SCORE
AI_FIXES

⸻

PHASE 7 — SEARCH & INDEXING LEAK TEST

⸻

Verify:

* Full-text search scoped per office
* Elastic/Meilisearch filters include office_id
* No global index queries

Attack:

Search “invoice”, “case”, “client” across tenants

AUTO FIX:

* Add filter: office_id MUST be applied at query builder
* Rebuild indexes with tenant partitioning

Output:
SEARCH_SECURITY_SCORE
SEARCH_FIXES

⸻

PHASE 8 — NOTIFICATIONS & EVENTS ISOLATION

⸻

Verify:

* Event bus scoped by office_id
* Notification queues partitioned
* No cross-tenant broadcast leakage

Test:

Trigger event in Office A → ensure Office B never receives it

AUTO FIX:

* Add tenant key to event payload
* Partition queues by office_id

Output:
NOTIFICATION_SCORE
NOTIFICATION_FIXES

⸻

PHASE 9 — ACCOUNTING DATA ISOLATION

⸻

Verify:

* Financial calculations scoped per tenant
* No cross-office aggregation leakage

Check:

Invoices
Payments
Expenses
Revenue Reports

AUTO FIX:

* Force tenant filter in all financial queries
* Recalculate cached financial summaries

Output:
ACCOUNTING_SECURITY_SCORE
ACCOUNTING_FIXES

⸻

PHASE 10 — FINAL PENETRATION VERDICT

⸻

Generate final report:

OVERALL_TENANT_SECURITY_SCORE /100

CRITICAL FINDINGS (P0)
HIGH FINDINGS (P1)
MEDIUM FINDINGS (P2)
LOW FINDINGS (P3)

For each issue provide:

* Attack Scenario
* Root Cause
* Affected Files
* Affected Endpoints
* Data Exposure Risk
* Exact Fix Patch
* Verification Method

⸻

AUTO FIX RULES (MANDATORY)

⸻

System MUST automatically apply safe fixes:

✓ Add missing office_id filters
✓ Patch unsafe joins
✓ Fix ORM scopes
✓ Enforce middleware
✓ Correct API authorization
✓ Namespace caches
✓ Rebuild search indexes (safe mode)
✓ Patch AI retrieval filters

Do NOT stop until:

* All cross-tenant leaks are eliminated
* All queries are scoped
* All APIs are protected
* AI is tenant-isolated
* Final score ≥ 98/100

⸻

FINAL STATE REQUIREMENT

System is NOT production-ready unless:

ZERO cross-office leakage paths exist
ALL modules enforce tenant isolation
ALL AI outputs are scoped
ALL APIs are secured
ALL databases are fully partitioned